/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images:{ domains:["img.freepik.com","source.unsplash.com","images.unsplash.com"]},
    optimizeFonts: false,
  
}
