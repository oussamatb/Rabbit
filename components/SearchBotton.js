export default function SeachBotton(props){
    return (
        <div className="ml-4 mt-4 p-2 border border-grayyy text-grayyy  text-sm px-8 rounded-sm transition duration-150 
        transform hover:scale-105 corsur animate-appeare animation-delay-200" onClick={props.onClick}>Search

        </div>
    )
}